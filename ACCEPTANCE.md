# 验收与回归用例

## 门槛与证据

每个用例写状态 `NOT_RUN / PASS / FAIL / BLOCKED`，附命令、seed、配置摘要、截图或日志路径。现在全部为 NOT_RUN；任务书本身不算执行证据。容差是建议验收目标，需先完成有效时间/视口对齐。

## A. 素材与场景（G0–G1）

| ID | 操作 | 可核验结果 |
|---|---|---|
| A01 | 下载两段新录像，拼接长片原件 | 文件字节数和 SHA-256 与 manifest 相同，ffprobe 可解码 |
| A02 | 查看 0、1、8、10、13 秒关键帧 | 时间按 PTS；数字不引用压缩缩略图做最终读数 |
| A03 | 从 L 静态帧逐个标节点 | 区域/普通/特殊节点均有 ID；初步约12点得到复核结论 |
| A04 | 按游戏视口而非外部视频黑边缩放 | 地图位置、大小、留白与原帧一致，有裁切参数 |
| A05 | 同一场景 5 种宽高比 | 所有节点可操作、文字不裁切、无横向滚动 |
| A06 | 显示原帧叠加对照 | 地图拓扑正确，节点中心误差目标≤短边1.5% |
| A07 | 对照截图关闭游戏图层 | 能明确区分真实交互图层，原录像不能作为伪游戏背景 |

## B. 指令与兵流（G1–G2）

| ID | 操作 | 可核验结果 |
|---|---|---|
| B01 | 从己方拖向合法敌方 | 指令只产生一次；出现首兵、队列和尾兵 |
| B02 | 从敌方拖拽、释放空白、pointercancel | 不发兵，不残留选中路径，不扣兵 |
| B03 | 使用100驻军，80预定预算，10兵/秒，无生产 | 3秒发出30；驻军70，预留50，在途+到达合计30 |
| B04 | B03后取消剩余队列 | 驻军仍70，预留0，未凭空返还50 |
| B05 | 同源连续提交两条命令 | 第二条只能使用真实可支配兵力，预留总数不超过驻军 |
| B06 | 排队中源被占领 | 原源未发队列按策略清理；已发兵仍是原阵营 |
| B07 | 两条路程100与200，匀速50/秒 | 首兵时间约2与4秒，加上明示发射延迟；不同帧率结果一致 |
| B08 | 200兵逐批攻击静止100守军 | 目标数字随抵达连续减少；不是尾兵到时一次清空 |
| B09 | 目标在部队途中改为友军 | 抵达当时增援，不按旧身份继续扣兵 |
| B10 | 弯路径三段，观察大队列 | 前后排依序转弯，不整体横移、瞬移或反向拉伸 |
| B11 | 测试2/20/200/600兵行军 | 视觉密度有差异，逻辑总数和伤害不受降采样影响 |
| B12 | 按钮区域拖动与地图拖动交错 | UI 不吞掉正常拖动，地图不会收到按钮指令 |

## C. 战斗与生产（G2）

| ID | 操作 | 可核验结果 |
|---|---|---|
| C01 | 8攻击包进入3守军 | 占领后5驻军，双方损耗总计6 |
| C02 | 3攻击包进入8守军 | 原主保留5驻军，攻击包清空 |
| C03 | 5攻击包进入5守军 | 按明确的平局策略执行；下一包行为可预测 |
| C04 | 两阵营同tick抵达中立 | 稳定排序可复盘，不因帧渲染顺序决定归属 |
| C05 | 250软上限节点收到80友军 | 变330；下tick不回落250；自然生产暂停 |
| C06 | C05派出100 | 变230后恢复生产，余数规则符合契约 |
| C07 | 产率1.47连续100游戏秒，无上限限制 | 累计应产147，误差不随渲染帧率积累 |
| C08 | 未接通特殊圈效果 | 图标可见但不暗中造成未声明的伤害/增益 |
| C09 | 奖励回调同ID触发2次 | 只加一次兵/金币；取消不发奖 |
| C10 | 清空领地但保留在途兵 | 按存活策略继续，不能提前结算后又改色 |
| C11 | 多轮生产、派兵、占领、取消、奖励 | 每tick总量守恒，负数/NaN/悬空owner为0 |
| C12 | 同seed+配置+输入在30/60/120渲染fps | 逻辑快照相同（排除渲染数据和墙上时间） |

## D. AI、存档与完整流程（G2–G3）

| ID | 操作 | 可核验结果 |
|---|---|---|
| D01 | 三阵营无人干预运行若干seed | 扩张并相互交战，AI不重复预留超额兵，不发0兵 |
| D02 | 被选目标已有大量友军在途 | AI估算避免无意义过量投入，原因可读 |
| D03 | 点击暂停30秒后恢复 | 模拟时间不跳30秒、不瞬间多出兵/奖励 |
| D04 | 切后台、锁屏、旋转恢复 | 单个模拟循环，触控坐标正确，不补算后台战斗 |
| D05 | 连续重试10次 | 只有一套监听/定时器，内存不随次数单调无界增长 |
| D06 | 同局重复结算触发 | 奖励只结算一次，重开计时/队列/状态干净 |
| D07 | 存档缺失、旧版本、非法数值、写入失败 | 正常提示/恢复，游戏仍能开始，不丢用户合法余额 |
| D08 | 关闭开发模式的新会话开局到结算 | 不依赖调试开关、假登录或手工改状态 |

## E. 视觉与真人体验（G3）

五组同视口对照：开场、单路兵流、多路交战、夺取、后期高密度。每组有录屏PTS、实现截图、配置与差异说明。优先看地图形状、位置、队列、数字连续性、改色与UI构图。

首/尾兵与夺取时间的目标误差≤10%；队列宽度/尾长目标误差≤15%。若原录像指令不可复现，不能给出假精确误差；改用可比局部观测并说明限制。

在用户同类安卓手机与至少一个桌面浏览器验证拖拽命中、空白取消、点选辅助、暂停和结算。真人反馈至少记录：是否看懂兵力、误触次数、指令延迟感、是否看清路线、是否像蓝本。一个人试玩不等于验证留存/付费。

## F. 性能与交付（G3及以后）

- 场景S：单路200兵；场景M：12节点、多路1000逻辑兵；场景L：多路5000逻辑兵，视觉数量按设置记录。
- 指标：设备、系统/浏览器、视口、DPR、平均/p95帧时间、活动小包、视觉实例、运行5分钟的内存趋势、命令到首个视觉反馈延迟。
- 参考目标：60Hz手机平均≥55fps，困难阶段不持续低于30fps；指令反馈目标≤100ms。未实测必须写NOT_RUN，不能把目标写成结果。
- 打包产物可以离线启动或明确列出网络依赖；没有账号密钥也应能完整玩单场；只有真实平台接口成功才标“广告/分享已接通”。
- 本轮第一交付的完成定义：G0、G1、G2相关用例通过 + G3对照报告完成 + 剩余差异透明。真实运营上线是后续不同门槛。
