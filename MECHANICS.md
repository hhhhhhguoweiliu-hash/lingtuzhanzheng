# 机制辨识与实现契约

这张表是研究任务，不是原游戏规则表。`观测`意味着画面可见；`候选`意味着待区分解释；`临时默认`只服务研究版本。每条结论保留来源时间窗，不能把候选自动升级为确定规则。

## 竞争假设

| ID | 问题与当前依据 | 需要区分的候选 | 最小验证方法 | 未确认时实现方式 |
|---|---|---|---|---|
| U01 | 兵流持续产生 | 单次预算排队 / 持续通道 / 长按连发 | 对照触摸结束、出发地扣兵、尾兵停止时刻 | 固定预算逐批发射；策略可替换 |
| U02 | 派出多少 | 全部 / 固定比例 / 手势决定 / 保留驻军 | 无生产短区间计算累计离开人数和剩余数 | 比例仅用候选 1.0，配置注明未实测 |
| U03 | 扣兵时点 | 命令时 / 每包发射时 / 隐藏预留 | 对照源数字变化与首尾兵出发 | 发射扣兵，预留占用可派额度 |
| U04 | 抵达结算连续 | 每单位 / 小包 / 周期性伤害 | 低速单路冲击与目标数字逐帧表 | 包大小 1 为逻辑基线，渲染可合并 |
| U05 | 路径限制 | 全图可选 / 相邻 / 图寻路 / 特殊通道 | 同阵营跨多区、越海、穿敌领测试 | 路由策略可配，不宣称任意直线为真 |
| U06 | 普通节点产速 | 固定 / 节点等级 / 阵营升级 / 特殊加成 | 静置无入出兵窗口拟合 | 每节点独立有效 rate；无证据字段为空 |
| U07 | 中立是否生产 | 不生产 / 低速 / 特殊点例外 | 观察相同白色节点 10–30 秒 | 默认不生产并标候选 |
| U08 | 250 的含义 | 自产软上限 / 硬上限 / 等级差异 / UI 截断 | 平台期间增援与出兵后的恢复曲线 | softCap 候选 250；外援不削除 |
| U09 | 六边形和覆盖圈 | 射程 / 产兵范围 / 防御 / 装饰 | 行军穿圈是否减员，圈内产速差 | 先画外观，效果默认关闭 |
| U10 | +100 图标 | 即时奖励 / 条件奖励 / 广告 / 技能 | 点击前后与触发完整录屏 | reward adapter；演示入口独立 |
| U11 | 底部按钮 | 空投 / 主动技能 / 广告奖励 / 冷却 | 按下、选目标、成功/取消全流程 | 外观与交互可测；实效待定 |
| U12 | 顶部比例条 | 区域个数 / 面积 / 兵力 / 综合值 | 只增兵不占地与占大/小区比较 | 暂用区域数且在差异表说明 |
| U13 | 一次选多个源 | 支持扫选 / 单源 / 全选命令 | 输入完整录制和多源发射起点 | 基础单源；多源不擅自假定 |
| U14 | 路上相遇 | 互不影响 / 抵消 / 只特殊区交战 | 两股对向等兵短路径测试 | 默认无遭遇战 |
| U15 | 易主后未发队列 | 取消 / 转移 / 原主继续 / 无队列 | 源失守时观察还是否发兵 | 清理预留、不额外扣兵，已发兵保留阵营 |
| U16 | 0 兵与淘汰 | 平局保属 / 中立化 / 首个到达占领 | 正好抵消，随后双方小包 | 默认保原归属；回放记录顺序 |
| U17 | 玩家栏与力竭文字 | 真人 / AI拟人 / 观战 / 导出字幕 | 网络/房间入口、原屏与分享屏对照 | 明示 AI 演示，字幕不直接复制 |
| U18 | 视频时间与游戏时间 | 实时 / 加速 / 变速 / 剪辑 | 连续计时器、UI 动画、音画变化 | 用录屏秒标注，游戏时间换算留空 |

## 兵力记账契约（推荐实现）

自然生产、奖励、战斗损耗是修改总兵力的来源。占领只改变 ownership，不无端增兵。可支配兵力 = max(0, garrison − reserved)；reserved 只是 garrison 的子集，不得加入总量。

发射一个包 k 时：先确认 k <= reserved 且 k <= garrison，随后 garrison -= k，reserved -= k，inTransit += k。取消剩余队列只减少 reserved，不增加 garrison（兵从未离开）；源失守清理其预留，已经在途的包不变。

对齐任意两时刻的总量：

`驻军总和 + 在途总和 = 初始总和 + 累计生产 + 累计奖励 − 累计战斗损耗 − 明确记录的其他损耗`

1:1 抵消中，一次攻击抵消 d 个守军会消耗双方合计 2d 个兵。攻方剩余兵在占领后成为驻军。阵营总量因占领零驻军不变。

自然生产软上限只影响生成：

`spawn = min(本 tick 应产兵, max(0, productionCap − garrison))`

当 garrison >= productionCap 时 spawn = 0；不能把已有 garrison 直接改成 cap。产兵余数的截断/保留策略需全程一致且有测试。

## 配置生命周期

1. `observed` 只记录具体素材的可见事实，不直接驱动通用数值。
2. `candidate` 可用于研究场景，必须有 unknownId 与选择理由。
3. `calibrated_for_scene` 表示匹配指定窗口，通过独立窗口验证后才能使用；仍不代表全游戏通用。
4. `confirmed_by_interaction` 需要原游戏可控交互记录，附输入与输出。

后续 Codex 用实际测量更新配置时保留旧版本、观测窗口和误差，不直接把研究初值重命名为原版参数。
