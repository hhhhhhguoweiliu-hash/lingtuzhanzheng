# 《解压来一局》领土战争玩法复刻｜Codex 详细任务书 v2

> 这是开发交接规格，不是游戏已完成的报告。目标是忠实复现用户录屏中的领土战争体验。用户明确否定上一版通用据点原型，不能把“能玩”当作“复刻成功”。
>
> 本版替代旧项目书中的武断机制和数值；保留旧原型作为反例与技术参考。最终以新录屏、可复查的测量和用户验收为准。

## 0. Codex 先读这几条

1. 从 `README.md` → `references/SOURCES.md` → 本任务书 → `MECHANICS.md` → `ACCEPTANCE.md` → `QUEUE.md` 开始。不要只读开工提示词就凭印象实现。
2. 首个必须做像的成品是长视频 `1000042572.mp4` 中的单场战斗。先还原地图、据点、兵流、数字变化与触控手感，再扩其他模式和局外系统。
3. 游戏名称来自用户分享文字；抖音是分享入口。尚未证明游戏只运行在抖音，也未确认原游戏的引擎、开发者、版本和后端。
4. 不把“兵流分批抵达”直接等同于“永久连线自动派兵”。出兵指令语义仍须辨识；渲染与结算不能再整批瞬时处理。
5. 125、200、250 是特定画面的观察值。不得无条件推广到所有关卡、阵营和等级；可见超过 250 的兵力意味着 250 不能直接写成全局硬上限。
6. 不将另一段视频的四人头像、昵称和表情当作真人联网的证据；第一阶段仍可由 AI 驱动，但必须诚实标注。
7. 所有未确定机制放到配置、实验和差异清单中。能独立推进的内容继续做，不能因一个未知规则停掉全部工作。
8. 本次交付是任务书和素材，不代表本次已经完成新游戏。完成 G2 后才能叫“单场复刻候选”，完成 G3 并通过真人试玩后才能叫“核心体验通过”。

## 1. 项目目标与交付定义

### 1.1 用户真正要什么

用户要依据自己提供的真实游戏画面，一比一接近地复现玩法、反馈、节奏和界面结构，然后逐步形成可继续开发的小游戏项目。优先级依次为：

- 操作和战斗规则可解释、可验证，符合录屏。
- 战场地图、军队密度、颜色、数字、动画和界面构图接近录屏。
- 单场对局完整、手机可操作、可复盘、可调参。
- 后续扩展关卡、成长、特殊据点、玩家对战形式和平台能力。

“一比一”不是已拥有源码、服务器或全部规则的声明。没有证据时，给出候选规则、选定临时默认、保留可替换接口，并明确剩余差异。

### 1.2 首个交付：长视频单场垂直切片

首选蓝、紫红、棕三个有色阵营与灰色中立区的场景。地图构图、起点、普通据点、特殊图标、覆盖圈、部队风格和上方比例条均须对照原帧。单场必须包含开局、扩张、多方交战、领地转换、两方收尾和结算。场景节点数量先逐个标注，初步观察约 12 个可见据点；未复核前不能硬编码为准确总数，也不能照搬旧原型的 32 个节点。

### 1.3 完整工程包

必须交付：可安装依赖的源码与锁文件、运行/构建说明、地图与参数配置、输入脚本回放、录像对照、性能记录、关键机制测试、差异清单、状态与下一步。用户拿到后能打开并试玩，下一位 Codex 能从断点继续。

## 2. 素材范围与使用方法

### 2.1 三段录屏的角色

- A：`1000042460.mp4`，之前成功读取约 213.14 秒、584×1280；包含非洲地图、首页/升级/任务/转盘/皮肤及一场多阵营战斗。当前工作副本报 `moov atom not found`，本次没有再次完整解码。历史总结可作为寻找内容的线索，不能当作逐帧精确测量。缺失原件属于外围页面复核的资料缺口。
- L：`1000042572.mp4`，257.034434 秒、720×1372；本次核心蓝本。可以观察三方、中立区、持续兵流、125 数字、200 数字、250 附近平台与超过 250 的数字、特殊据点/圆形覆盖区、+100 图标及后期颜色转换。
- S：`1000042571.mp4`，60.996496 秒、720×1372；另一地图和四人玩家栏。可见昵称、表情、聊天图标与“力竭了”文字。字幕可能来自分享加工，不应未核实就写入游戏 UI。

L 原文件超过当前上传通道单文件限制，提供两个二进制分片；按顺序拼接后校验 SHA-256 即得原始文件。另附压缩审阅副本（若生成成功）；审阅副本用于快看，正式读数字与测时使用重建原文件。精确链接、尺寸、哈希和重建命令在 `references/SOURCES.md`、`references/source-manifest.json`。

### 2.2 测量纪律

用解码时间戳 PTS 定位，不能根据容器给出的异常 `r_frame_rate` 推算帧数。转码帧率不代表原游戏逻辑帧率。录屏可能经过裁切、录屏器重采样或加速；先辨识，再将录屏秒换算为游戏秒。没有输入可视化时，不能仅凭兵流出现就断言触摸开始时间。

先做三个短区间：L 0–15 秒研究出兵；L 中段多方交战研究改色/抵达；L 后段研究高兵力和收尾。读取 S 的用途是模式差异核对，不混用两场的升级等级、产速和容量。

每条观察包含来源文件、PTS 区间、节点 ID、肉眼所见、遮挡/清晰度、可能解释、排除条件。原始观察与解释分列。OCR 只是候选结果，关键数字人工核对。比较容差见第 14 节。

## 3. 已知、未知与上一版纠偏

### 3.1 已知的画面现象

- 区域是不规则地理形状，不是均匀格子；领地随阵营变色，据点数字可持续变化。
- L 的三种有色阵营使用不同颜色和单位符号，军队以队列/兵流从据点向外移动；不能只用一个线段代替。
- L 约 8–13 秒，中立守军数字随来兵持续减少。必须支持分批抵达、分次扣减的表现与结算。
- L 约 1 秒画面，棕/紫红标记可见 200，若干中立点 125；蓝方已经发生行动，画面数字不能直接视为开局设定值。
- 部分节点出现六边形图标和圆形覆盖区；另有 +100 图标，底部有类似空投的圆形按钮。
- 顶部多色比例条随局势变化，但它统计领地数量、面积、兵力或综合势力尚未确定。

### 3.2 关键未知问题

参见 `MECHANICS.md` 中 U01–U18 的竞争假设。高优先级为：出兵语义、扣兵时点、连续结算、路线规则、产兵/容量、特殊区域效果、比例条分母。中优先级为：选兵范围、多源集结、路径穿越、遭遇战、占领后生产、淘汰条件。低优先级为：真实联机、广告奖励、隐藏数值成长公式。

### 3.3 上一版必须修正的地方

旧版任意直线派兵、瞬间扣除一半、单个 March 到终点整批结算、AI 和玩家固定起点、统一 100 上限、32 个 Voronoi 区域、通用首页弹窗和金币成长，均不能作为蓝本规格。可复用纯逻辑/绘制解耦思路和确定性测试；地图、数值、战斗命令、军队绘制及 UI 要重新设计。

不要先加 30 关、账号、排行榜、商店来掩盖核心不像。旧网页可继续作为历史参考，但不要直接覆盖，先保留可回退版本并建立重做分支。

## 4. 页面与输入体验

### 4.1 长视频战场页

- 主屏是战场；顶部返回按钮和长条势力条；中央地图；底部曲线/留白区及主要技能按钮。背景、地图占屏比例、起点分布以关键帧为准。
- 将纯游戏视口和录屏附加区域分开。720×1372 是视频尺寸，不必等同设备 CSS 像素；先测上下边界再定义设计空间。
- 禁止加入视频没有的巨大品牌标题、说明横幅、装饰卡片和常驻升级面板。教程只在首次输入前短提示，点击即可隐藏。
- 兵力文本始终可读，保持接近原版的相对尺寸与位置；高密度军队不能完全遮挡数字。暂停时也应能检查战场。

### 4.2 指令交互

实现单源拖拽的基础输入：按下己方可派兵区域/据点 → 进入选择 → 移动时出现可见路径提示 → 释放在合法目标发出命令 → 释放在空白取消。点选后点目标可作为易用性备选，但在验收中区分蓝本操作与新增辅助。

阈值使用 CSS 像素并经手机测量，不能随设备像素比改变误触率。UI 按钮与战场手势互斥；pointercancel、失焦、切后台、系统手势中断必须清理预览且不发出幽灵指令。派兵命令需做 owner、可用兵力、目标有效性校验。

若逐帧/实测证实多源扫选、长按连发、自动连线、改道等操作，再启用相应入口。所有输入最终转换为标准命令，不让触摸处理直接修改战斗数组。

### 4.3 暂停、失败和结算

暂停冻结逻辑和游戏计时；恢复不得把后台时间补算成瞬间出兵。失败应允许观看剩余战斗或重新开始（是否为蓝本行为需标记），重试保持同一配置与选定 seed。结算只触发一次；没有真实广告 SDK 时不展示可领取的“看广告奖励”。

## 5. 地图制作规格

### 5.1 标注与坐标

以 L 第一秒清晰画面为起点，为每个可见节点建立稳定 ID。记录区域轮廓、中心、兵力标签锚点、所有权、初始可见兵力、图标、覆盖区半径、遮挡范围。地图轮廓与节点必须使用同一坐标系；归一化存储，渲染时统一缩放和平移。附 `references/node-observations.csv` 初始观察，仅为待复核草稿。

不要用手绘四边形、随机 Voronoi 或网上任意非洲图替代本场地图。可描摹用户提供画面的几何关系供内部还原对比；对外发布所需地图素材另行重绘/确认来源，避免把游戏截图当成自己的成品纹理。无需先判断它代表哪个行政区才能制作场景。

### 5.2 区域与路径

数据层分开 territory polygon（显示/命中）、node anchor（兵力/出兵中心）、route graph（合法连接）、path polyline（单位路径）。视觉接壤不自动等于可出兵；视频直线也不能证明跨所有地块合法。配置支持 neighbor-only / any-visible / graph-path 等候选，默认值有状态标记。

小岛/碎片不能因区域面积小而丢失；多多边形共享节点的情况须可表达。检查轮廓自交、明显间隙、区域重叠、节点在区外、命中偏移和路径断开。

### 5.3 重放对比

渲染参考帧叠加/透明度滑块只放开发面板，不混入正常用户流程。提供固定 seed、固定命令序列、固定时间跳转和独立截图导出。不可把录屏作为动画背景伪装可玩的游戏。

## 6. 核心模拟架构

### 6.1 职责拆分

`sim/` 纯数据与事件；`input/` 将触控变为命令；`render/` 读取状态绘制；`maps/` 可验证地图；`config/` 参数；`platform/` 存档/音频/广告/分享适配；`tools/` 标注、回放、截图和模拟；`tests/` 关键守恒和交互回归。避免引擎对象、DOM、平台 SDK 混入模拟内核。

技术建议是 TypeScript 加成熟 2D 游戏引擎或可测试的 Canvas/WebGL 层。不是强制某框架/版本；根据仓库实际环境和目标端做一份简短选择记录，锁定依赖。网页预览只作为第一交付载体，不能把抖音分享链接当作必须使用抖音 SDK 的依据。

### 6.2 时间、状态与确定性

使用整数 tick、固定 dt、种子随机数、稳定事件排序。建议起始 dt=1/30 秒，渲染独立插值；这是实现建议，不是原版频率。生产使用定点数累积/余数，整数兵力单独维护。避免浮点误差导致不同帧率下多一兵或少一兵。

状态机：loading → ready → playing ↔ paused → won/lost → result。明确切场景、重试、存档迁移和异常恢复。控制器与渲染各自只有一个生命周期，重复开始不能叠加计时器。

### 6.3 命令、队列和兵力去向

至少区分：驻军 garrison、已预定待出发 reserved、正在移动 inTransit、已损耗 casualties。两种合法实现：预定仅占用可支配额度，实际发射再从驻军扣；或预定时转移到独立队列库存。选一种并全局一致，禁止重复扣减。

建议默认“固定预算分批发射”：命令产生预算，按发射速率逐小包发出，全部发完结束。保留“持续通道”策略以便测量证实后切换。该默认只是工程起点，不声明原版必如此。取消/改目标时尚未发出的兵如何返还、已在途兵是否改道要通过明确策略处理，不能销毁兵力。

### 6.4 每 tick 结算顺序

建议：读取带 tick 的命令 → 校验/排队 → 自然生产累计 → 队列生成 UnitPacket → 移动 → 处理沿途效果（若已证实）→ 抵达小包排序与逐包结算 → 变色/能力刷新 → 胜负检查 → 输出事件与快照。每步兵力变动带原因。排序采用 arrivalTick、subTick、sequenceId，规则记录在回放版本中。

如果两个敌方小包同 tick 抵达同一中立点，应验证并记录事件顺序偏差。不得为了便于实现固定让玩家先结算。必要时将所有权仲裁与当 tick 净交战分开，但要先明确测试期望。

## 7. 出兵、行军与战斗

### 7.1 出兵与兵流

一个命令必须产生一串沿时间展开的小包。小包可以代表 1–n 个逻辑兵，视觉实例数可压缩，但压缩不得改变到达时间、伤害和总兵数。发射间隔、每包人数、列数、行间距、横向间距、单位符号和尾部长度可独立配置。

前排先出发、先到达，后排仍在路上；不能把 200 人缩成一个跑点、整队统一倒扣。出发地数字应与选定扣除规则一致，目的地数字根据抵达/交战连续改变。

### 7.2 移动与转向

路径长度按折线弧长累计。速度以“设计坐标单位/游戏秒”定义，不能随分辨率变化。队列转弯跟随各自路径进度，不能所有点同时旋转横移。多条兵流交叉时的遮挡可用排序/透明度处理；是否发生途中遭遇战必须独立配置，未经证实默认不添加。

在屏幕上验证三种距离的旅行时间，不能只调最近一条边。记录首兵与尾兵抵达时间、前后队宽度、单路容量和同时活动的路线数。

### 7.3 基础战斗候选

临时基线是 1 攻 1 抵消，小包抵达就结算：同阵营增援加兵；异阵营削减守军；攻击超过守军则换所有权并留下差额。恰好相等时归属为未定候选，默认保留原归属且驻军为 0，需测试并注明。增援被截获、原据点失守、目标改色时，已发出小包保留原阵营身份，按抵达时真实目标归属处理。

显示数字、可用兵力、自然生产、预定额度不能混为同一个字段。敌方刚占领不应瞬间额外获得一份初始兵力。

### 7.4 胜负

领地全失但仍有在途兵/独立队列时，是否淘汰需先定义。建议在“无可恢复的生产据点且无该阵营在途有效兵”时淘汰；原据点失守导致未发队列归属如何处理由队列策略决定。单场胜利是只剩玩家这一有色阵营仍存活，中立地块是否必须全部占领列为可配规则。S 中“力竭了”可能是旁观/淘汰或分享字幕，不能直接推断失败条件。

## 8. 生产、容量与特殊区域

### 8.1 生产率辨识

寻找不出兵、不受攻击、无增援的节点片段，拟合 ΔN/Δt。对普通、刚占领、特殊圈内、不同阵营分别测量；若存在升级状态未知，写成该场景的有效产率，不反推基础公式。中立区是否产兵使用多个静置时间窗确认。

### 8.2 容量模型

区分自然生产软上限、增援是否可超过上限、硬容量和显示截断。看到 250 平台又看到高于 250，优先测试“自然生产到 250 停止，增援可超过”，但也保留不同节点/等级容量解释。禁止每 tick `min(cap,army)` 把合法增援蒸发。上限变化、技能失效、升级读取都必须有明确处理。

### 8.3 特殊据点/覆盖圈

先保证六边形、覆盖圈、图标位置和动画能呈现；再区分其实际作用：产兵加成、射击/伤害、射程、减速、装饰提示、技能目标。每个候选都有启用开关和测试场景。未确认阶段可以在开发工具里显示候选效果，正式试玩说明中写“效果待核实”，不发明已经确认的炮塔规则。

### 8.4 +100 与底部按钮

+100 只证明有这个视觉提示，不能证明点击即免费加兵或广告成功。实现通用 RewardGrant/SkillCommand 接口，demo 奖励走显式演示入口，不接入真实广告。若暂用“选目标后增援 100”的研究模式，注明该行为是模拟假设；来源、冷却、一次性限制、可选目标、空投时间都单独配置。重复回调只发一次奖励。

## 9. 敌方 AI 与难度

不要把“AI 故意犯错 15%”“思考 2.5 秒”“60–70% 首胜”等旧讨论数字当作原版事实。AI 第一目标是复现扩张节奏与可观察行动，不是最强策略。

AI 分为候选目标生成、估算到达时守军、预算检查、行动打分、下令冷却。评分项含路径耗时、目标产能、净兵力优势、已在途己方兵、其他敌军威胁、地图前线价值。所有输入按可比尺度归一化，避免距离数字天然压倒别的项。

禁止无限叠加同一目标的预定军、超额扣兵、无资源时持续发零兵命令。防守/增援只转移真实兵力。不同 AI 阵营应该能互打，不默认联合围攻玩家。决定不做某件事也要有调试原因。

先用一套参数复现长视频的三方阶段与后期两方拉锯。测试队列包括扩张型、保守型和集中进攻型虚拟玩家。机器人胜率仅说明这些策略在模拟中的结果，不等于真实用户首胜率。

## 10. 军队渲染与视觉验收

### 10.1 应还原的视觉信息

地图长宽比与屏幕位置、区域轮廓与边界、三阵营配色、普通/特殊据点符号、数字锚点、单位符号差异、队列宽度与尾长、选中指示线、占领变色顺序、覆盖圈、顶部比例条、底部主要按钮。

主体应始终可操作，不能以“艺术升级”为由换成另一个风格。蓝色单位像圆点/球状、其他阵营有各自符号的区别要保留在配置中，不要求照抄受保护的美术原文件。

### 10.2 性能设计

建议批量绘制、精灵图集/实例化、对象池、避免逐兵 DOM。逻辑兵和视觉兵数量映射记录在调试栏。低性能时降视觉密度、特效和阴影，不能降低实际发兵/伤害。稳态渲染不要每帧重建比例条 DOM 或触发全页布局。

参考验收目标为手机 60Hz 屏平均 55fps 以上，困难场景不持续低于 30fps；具体设备/浏览器/分辨率/路线数/视觉实例数必须记录。逻辑压力场景与视觉压力场景分开，禁止只在空地图测速。

### 10.3 音效和辅助信息

未测出原音效时使用轻量临时音效并可静音，不声称还原。触觉反馈仅在平台支持时启用。色彩之外用阵营图标/选中轮廓辅助识别。数字在正常屏幕尺寸可读，不用过小字体为地图腾空间。

## 11. 数值配置与平衡流程

`config/research-candidate.json` 是试验初值，字段旁有机制状态；它不是已拟合参数。首先关闭局外成长和广告影响，只拟合 L 一场的有效参数，然后研究 A 的升级信息。

可调参数分组：时间刻度、production、capacity、dispatch、packet、movement、combat、special、AI、victory、render。单次实验只改变一组，输出 config hash、seed、命令脚本、变化项和观测结果。

用观测轨迹拟合：N_source(t)、N_target(t)、首/尾兵抵达、夺取时刻、区域面积或占有数、队列宽度。对未出现的输入不能假定玩家命令；人工复现同类操作并比较局部片段。

先筛候选机制再微调参数。错误机制也可能通过任意数值拟合一小段，必须用不同距离、不同兵量、敌方与玩家片段交叉验证。数据不足时保留多个候选，不造一个“唯一真实公式”。

## 12. 第二阶段：关卡与外围系统

通过单场体验门槛后再做 S 的另一地图、四阵营与玩家栏外观，以及 A 的首页/成长/任务/转盘/皮肤。不要提前抹平不同模式的差异。

关卡数据应包含 mapId、规则集、阵营初始状态、特殊节点、AI 参数、目标与奖励；5 关必须有布局或目标差异，不能只加敌军数字。30 关不是当前第一阶段验收范围。

局外升级要记录当前等级、当前/下一值、费用、货币余额与存档事务。原先 `10+L`、`1.05^(L−1)`、`100×1.18^(L−1)` 只能在研究草案里保留，不是测得公式。未读取升级录屏前不固定成长体系。

任务和转盘分别定义进度、领取、刷新、奖励幂等与随机概率。概率不能由 UI 扇区面积偷推。皮肤先纯视觉，若有属性需明确。商店与广告先接适配层和演示回调，真实支付/广告需对应平台能力及账号。

## 13. 玩家对战、录屏和分享

### 13.1 玩家栏模式

第一步复刻玩家栏、阵营色、表情位置与聊天按钮可交互形态，说明当前为 AI 演示。必须在决定做真人联机前核实原版连接条件和用户目标。

若需要联机，另立里程碑：服务端权威指令校验、房间匹配、观战、断线重连、服务器时钟、快照/差分、输入顺序、反作弊基本校验、结束结算。不得让客户端直接上传任意最终兵力；不得用随机昵称 AI 冒充真人。

### 13.2 录屏与分享

用户指出这些视频由内部录屏软件分享成抖音。因此应记录“录制/导出/分享”为后续完整体验的候选模块，但它不能阻塞核心战斗。先核对录屏器究竟是游戏自带、宿主平台还是系统工具。

后续可实现本地回放录制或画布视频导出：用户主动开始/结束、显示时长和失败状态、切后台行为明确、音轨和分辨率可选。平台原生分享另做适配；未取得能力时支持下载视频，不显示虚假的“已分享到抖音”。禁止将用户原录屏直接变成我们游戏的宣传实机。

## 14. 验收方法与容差

验收分为三个独立结论：机制是否自洽且符合观测；画面/操作是否接近；是否已经完整上线。不能用逻辑测试通过替代画面通过，也不能用部署成功替代游戏通过。

建议数值容差作为初始门槛：清晰整数读数比对不超过 1 兵或记录的 OCR 不确定度；首兵/尾兵抵达与夺取时间在可对齐片段误差不超过 10%；屏幕上节点中心误差不超过游戏视口短边的 1.5%；队列宽度/长度在相同兵量下误差先控制在 15%。这些是项目目标，不是已测出的精度。若录屏有加速/遮挡先修正条件，再决定阈值是否适用。

战斗路径测试、跨帧率复盘、资源守恒、分批抵达、取消命令、易主后在途兵、重复奖励、后台暂停、移动端拖拽、地图轮廓、性能采样均在 `ACCEPTANCE.md` 给出可执行检查。

截图对照必须用同一视口裁切和缩放规则；不只比较开场静态。至少给出开场、单路出兵、三方混战、攻占瞬间、后期大兵团五组对照。自然随机对局没有同输入时，不要求逐帧像素一致；用受控脚本作工程验证，用录屏现象作形态验证。

## 15. 实施里程碑与工作量口径

- G0：素材下载/完整性、节点标注、机制候选。验收是可复查观察表与不会混淆事实的规则草案。
- G1：本场静态战场、输入、单路分批兵流和可见数字变化。验收是手机可操控且形态接近。
- G2：三阵营完整对局、特殊元素骨架、胜负/回放/调试工具。验收是单场闭环且机制测试通过。
- G3：视频校准、人工试玩、帧率/内存/输入修复。验收是五组录像对照及明确差异报告。
- G4：扩 S、A 的其他地图和外围系统。
- G5：已确认的平台 SDK、真实分享/广告、可选真人联机和发布。

不预先承诺“一天完整复刻”或“几小时一比一”。G0 完成后按地图描绘量、规则不确定项和可用素材重估人日，报告区间与依据。当前无人值守队列只自动推进已明确、可逆的素材/内核/原型任务；平台凭据、付费和发布按该执行环境的既有授权处理。

## 16. 交接与断点要求

每个稳定检查点更新 `STATE.md`、`QUEUE.md`、`DIFFERENCES.md`；记录真实提交 SHA、测试命令及结果、config hash、seed、关键录屏路径、下一项。不要把计划写成已完成。

本仓库是用户指定的 `hhhhhhguoweiliu-hash/lingtuzhanzheng`。当前任务书不向 meta 写入游戏内容。后续开发先读取实际仓库规则和工作区状态，不覆盖用户工作。

如素材链接需要登录、文件缺失、账号不可用：在 `BLOCKED.md` 记录具体依赖与可继续任务。不得只回复“请提供更多信息”然后停工；能做规则测试、可替换配置和已知地图部分就继续。

## 17. 最终提交清单

- 可试玩链接或可运行构建包，注明运行平台和实际测试设备。
- 完整源码、锁文件、许可证/素材来源记录与一键运行步骤。
- 一场长视频对应场景的配置、地图、规则候选选择与待测问题。
- 对照录屏/截图、输入回放与数据日志；没有测量的数据保持空值。
- 核心测试与性能报告；从没有手工调试数据的浏览器新会话验证开局到结算。
- 与蓝本仍不同的地方、被临时模拟的能力、真实联机/广告是否接通。
- 下一步只列真正未完成项。用户最终验收“像不像、好不好操作”，不让用户承担程序内部验错工作。
